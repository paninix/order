/**
 * 用户模块
 */

import {CUSTOMER} from '../types' //从types.js中引用常量

 const state={
 }

var getters={
   
}

const actions = {
    
}

const mutations={
   
}

export default {
    state,
    getters,
    actions,
    mutations
}